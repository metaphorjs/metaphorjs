
* When building templates - extract all attributes and store them dehydrated,
leaving only regular attributes without expressions and some identifier with
which we will then restore stuff
*? combine all scope's expressions into one function that updates the whole scope
* when building templates - store expressions in a map; allow expressions with pipes
* rewrite to es6 + babel

* parentCmp stack in app
*? directive that calls component's function to init current node
    (request sub component creation for example)
